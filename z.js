function isValidname(firstname) {
    if (
        typeof firstname !== "string" ||
        /[0-9]+/g.test(firstname)


    ) {   "Aman Is Prajapat"    /[A-Z]/gi " $1"
        return false; 
    }
    return true;
}
console.log(isValidname("aman5"))

//     //
function isValidname(firstname) {
    if (
        typeof firstname !== "string" ||
        /[0-9]+/g.test(firstname)


    ) {
        return false; 
    }
    return true;
}